var otherUpgrades = [

    // invincible bullets
    _.defaults({
    }, Upgrades.defaults),

    // sheild
    _.defaults({
    }, Upgrades.defaults),

    // bomb
    _.defaults({
    }, Upgrades.defaults),
];